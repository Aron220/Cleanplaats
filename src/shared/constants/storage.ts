export const STORAGE_KEYS = {
  settings: 'cleanplaatsSettings',
  panelState: 'panelState',
  firstRun: 'firstRun',
} as const;

export const LOCAL_STORAGE_KEYS = {
  darkMode: 'cleanplaats:darkMode',
} as const;
